@model PizzeriaManagementApp.ViewModels.PizzeriaEmployeesVM

<form method="post" asp-action="Create">
    <input asp-for="IdPizzeria" hidden />
    <div class="border p-3">
        <div class="form-group row">
            <h2 class="text-info p-3">Add employees to pizzeria</h2>
        </div>
        <div class="row">
            <div class="col-8 offset-2">
                @if (Model.EmployeesCheckBoxList.Count > 0)
                {
                    <div class="form-group row">
                        <table class=" table table-bordered table-striped" style="width:100%">
                            <thead>
                                <tr>
                                    <th style="width:20%">
                                        Is selected
                                    </th>
                                    <th>
                                        Employee name
                                    </th>
                                </tr>
                            </thead>
                            @for (int i = 0; i < Model.EmployeesCheckBoxList.Count(); i++)
                            {
                                @Html.HiddenFor(p => p.EmployeesCheckBoxList[i].Id)
                                <tr>
                                    <td>
                                        @Html.CheckBoxFor(p => p.EmployeesCheckBoxList[i].IsChecked)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(p => ((Employee)p.EmployeesCheckBoxList[i].Object).FirstName) @Html.DisplayFor(p => ((Employee)p.EmployeesCheckBoxList[i].Object).LastName)
                                    </td>
                                </tr>
                            }
                        </table>
                    </div>
                }
                else
                {
                    <p class="text-center">All possible employees are already assigned to this pizzeria</p>
                }
                <div class="form-group row">
                    <div class="col-8 offset-2 row">
                        @if (Model.EmployeesCheckBoxList.Count > 0)
                        {
                            <div class="col">
                                <input type="submit" class="btn btn-success w-100" value="Add" />
                            </div>
                        }
                        <div class="col">
                            <a asp-controller="PizzeriaEmployees" asp-route-Id="@Model.IdPizzeria" asp-action="Index" class="btn btn-primary w-100">Back</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-4">

            </div>
        </div>
    </div>
</form>