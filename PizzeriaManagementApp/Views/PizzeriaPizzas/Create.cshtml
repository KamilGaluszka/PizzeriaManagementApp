@model PizzeriaManagementApp.ViewModels.PizzeriaPizzaVM

<form method="post" asp-action="Create">
    <input asp-for="IdPizzeria" hidden />
    <div class="border p-3">
        <div class="form-group row">
            <h2 class="text-info p-3">Add pizzas to pizzeria</h2>
        </div>
        <div class="row">
            <div class="col-8 offset-2">
                @if (Model.PizzasCheckBoxList.Count > 0)
                {
                    <div class="form-group row">
                        <table class=" table table-bordered table-striped" style="width:100%">
                            <thead>
                                <tr>
                                    <th style="width:20%">
                                        Is selected
                                    </th>
                                    <th>
                                        Pizza name
                                    </th>
                                    <th>
                                        Pizza size
                                    </th>
                                    <th>
                                        Pizza thickness
                                    </th>
                                </tr>
                            </thead>
                            @for (int i = 0; i < Model.PizzasCheckBoxList.Count(); i++)
                            {
                                @Html.HiddenFor(p => p.PizzasCheckBoxList[i].Id)
                                <tr>
                                    <td>
                                        @Html.CheckBoxFor(p => p.PizzasCheckBoxList[i].IsChecked)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(p => ((Pizza)p.PizzasCheckBoxList[i].Object).Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(p => ((Pizza)p.PizzasCheckBoxList[i].Object).Size.Name) - @Html.DisplayFor(p => ((Pizza)p.PizzasCheckBoxList[i].Object).Size.Value)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(p => ((Pizza)p.PizzasCheckBoxList[i].Object).Thickness.Name)
                                    </td>
                                </tr>
                            }
                        </table>
                    </div>
                }
                else
                {
                    <p class="text-center">All possible pizzas are already assigned to this pizzeria</p>
                }
                <div class="form-group row">
                    <div class="col-8 offset-2 row">
                        @if (Model.PizzasCheckBoxList.Count > 0)
                        {
                            <div class="col">
                                <input type="submit" class="btn btn-success w-100" value="Add" />
                            </div>
                        }
                        <div class="col">
                            <a asp-controller="PizzeriaPizzas" asp-route-Id="@Model.IdPizzeria" asp-action="Index" class="btn btn-primary w-100">Back</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-4">

            </div>
        </div>
    </div>
</form>